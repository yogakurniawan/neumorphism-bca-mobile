import { position, maxWidth, display, borderRadius, height, width, space, color, textAlign } from 'styled-system'
import styled from 'styled-components'

export default styled.div`
  ${space}
  ${position}
  ${height}
  ${width}
  ${maxWidth}
  ${display}
  ${color}
  ${textAlign}
  ${borderRadius}
  ${position}
`
